# Codeception Test Suite Configuration

# suite for acceptance tests.
# perform tests in browser using the WebDriver or PhpBrowser.
# If you need both WebDriver and PHPBrowser tests - create a separate suite.

class_name: AcceptanceTester
modules:
    enabled:
        - WebDriver
        - ConfigHelper
        - Asserts
        # - DrupalDrush
        # - DrupalVariable
    config:
        WebDriver:
            url: 'http://example.dev'
            browser: 'firefox'
            window_size: '1024x768'
        CiviRemoteApiHelper:
            path_civicrm_settings: '/Users/chris/Sites/commerce.dev/sites/default/civicrm.settings.php'
            path_civicrm_api: '/Users/chris/Sites/commerce.dev/sites/all/modules/civicrm/api/class.api.php'
        ConfigHelper:
            site_key:       'SITEKEY'
            admin_apikey:   'APIKEY'
            admin_username: 'some.administrator'
            admin_password: 'some.adminpass'
            demo_username:  'some.regularuser'
            demo_password:  'some.regularpass'
